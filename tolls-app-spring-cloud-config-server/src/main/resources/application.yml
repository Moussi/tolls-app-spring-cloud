---
server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Moussi/tolls-app-spring-cloud
          search-paths: tolls-app-config/*
          #username: username
          #password: passwrd
